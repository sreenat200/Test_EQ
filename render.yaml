services:
  - type: web
    env: python
    name: eq-assessment
    pythonVersion: 3.10.13
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn eq_assessment.wsgi:application
